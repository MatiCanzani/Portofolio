---
const { link } = Astro.props

interface Props {
  link: string;
}
---

<a href={link} class="px-2 text-md uppercase  -rotate-90 transition-color transition-shadow duration-300 ease-in-out text-slate-50 hover:shadow-slate-50 hover:scale-125">
  <slot>
    Text for a link page
  </slot>
</a>